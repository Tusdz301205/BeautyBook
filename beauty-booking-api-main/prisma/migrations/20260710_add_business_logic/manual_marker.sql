-- Migration metadata for "add_business_logic"
-- Applied via direct psql since Prisma migrate cannot run safely in this AI session.
-- This file is kept so Prisma migrate history is in sync.
SELECT 1;
